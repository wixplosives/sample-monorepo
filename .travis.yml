language: node_js
sudo: required

node_js:
  - 10
  - 8

addons:
  firefox: latest
  chrome: stable

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.6.0
  - export PATH=$HOME/.yarn/bin:$PATH
  
cache:
  yarn: true
